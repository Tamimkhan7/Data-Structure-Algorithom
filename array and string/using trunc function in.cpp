#include <bits/stdc++.h>
using namespace std;
int main()
{
    double x, result;
    cin >> x;
    cout << trunc(x) << endl; // trunc and round function they are similar, we can use one of these function
    cout << round(x) << endl; // round function we can use basically number round korte, jar kacakaci hobe oi value ta dekhabe
    cout << floor(x) << endl; // The floor() function in C++ returns the largest possible integer value which is less than or equal to the given argument.
}